# قائمة الميزات — المنجز مقابل ما يحتاج API خارجي

## P0 — التشغيل الأساسي: منجز بالكامل وفعّال
- تسجيل دخول حقيقي (كلمات مرور مشفّرة Werkzeug PBKDF2)، صلاحيات (RBAC) حقيقية بـ 7 أدوار
- إدارة اللاعبين، أولياء الأمور (بربط متعدد الأبناء)، المدربين، المجموعات، الباقات
- محرك اشتراكات كامل مع الفصل الصارم بين Subscription Period و Session Entitlement
- الحصص التعويضية والإضافية وحصص اللاعبين القدامى (Legacy) بسياسة صلاحية قابلة للتعديل وسبب
  إلزامي لكل منح
- خصم تلقائي عند الحضور بمنطق FEFO (الأقرب انتهاءً أولًا)، حماية كاملة من الخصم المضاعف
- إلغاء تحضير يعكس الحصة الصحيحة بالضبط (لا رصيد عشوائي)
- تحضير بالباركود الحقيقي (Code39، وليس رمزًا وهميًا) + بحث بالاسم/الكود + تحضير جماعي
- لوحة تحكم بكل KPIs المطلوبة + قسم "يحتاج تدخل اليوم" الحقيقي (مبني على استعلامات فعلية،
  لا بيانات معلَّبة)
- سجل حركة الحصص (Session Ledger) وسجل تدقيق (Audit Log) لكل عملية حساسة
- مركز تجديدات (Renewal CRM) بأربع فئات فعلية + مخاطر تجديد Rules-Based شفافة (ليست AI مزيّفة)
- مركز تقارير One-Click: عرض HTML + تنزيل CSV + تنزيل PDF عربي RTL حقيقي (عبر Chromium)

## P1 — التطوير والتحفيز: منجز
- تقييم أولي/دوري/سريع بأربعة محاور وأوزان قابلة للتعديل، مع عرض مختلف للطفل (لغة محفزة) وللإدارة (أرقام)
- مستويات فوق (5 مستويات) وجاهزية ترقية محسوبة فعليًا (حضور + تطور + انضباط)، تحتاج اعتماد مشرف
- رصيد فوق (Wallet + Ledger حقيقي، لا يمكن أن يصبح سالبًا، وحد يومي للمدرب)
- إنجازات مبنية على قواعد صريحة (أول حصة، 5 متتالية، 10 بانضباط، قفزة تطور، أول ترقية، أول
  100 نقطة، 3 تجديدات) — تُفعَّل تلقائيًا عند الأحداث الحقيقية
- متجر فوق بمخزون حقيقي وworkflow استبدال كامل (Pending → Approved → Ready → Completed) مع
  عكس صحيح عند الإلغاء
- بوابة ولي الأمر (تبديل بين أبناء متعددين) وبوابة اللاعب (Career Mode Hub)

## P2 — النمو والتحليلات: منجز (نطاق مبسّط مقصود)
- CRM Leads كامل (حالات، مصدر، حملة، متابعة) + Trial Management → تحويل لعضوية
- الإحالات (Referral) بحالاتها الكاملة
- مركز إجراءات الاحتفاظ (Retention Action Center) بسبع فئات فعلية
- Legacy Players و Conversion Pipeline (لا تحويل تلقائي لعضوية مدفوعة)

## ميزات تحتاج اتصال API خارجي حقيقي (بنية جاهزة، غير موصولة فعليًا الآن)
هذه البنود مصمَّمة بحيث تُفعَّل بربط مزوّد خارجي فقط، دون أي تعديل في قاعدة البيانات:
- **إشعارات WhatsApp / SMS** — `business/notifications.py` يُنشئ التنبيه داخل النظام فعليًا
  (Notification Center يعمل بالكامل)، لكنه لا يرسل رسالة WhatsApp/SMS حقيقية لأن ذلك يتطلب
  حساب مزوّد (مثل Twilio أو 4jawaly) ومفاتيح API لا تتوفر في هذه البيئة. نقطة التوصيل الوحيدة
  المطلوبة: دالة `send_whatsapp(to, message)` تُستدعى من نفس المكان الذي يُنشئ فيه الإشعار حاليًا.
- **البريد الإلكتروني التلقائي** — نفس الوضع؛ يحتاج مزوّد SMTP/Mailgun/SES حقيقي.
- **الدفع الإلكتروني المباشر (بوابة دفع)** — النظام يسجّل حالة الدفع والمبلغ المدفوع والمرجع،
  لكن تحصيل الدفع نفسه (مدى/Apple Pay أونلاين) يحتاج ربط بوابة دفع سعودية (مثل Moyasar/HyperPay).
- **إشعارات فورية على الجوال (Push Notifications)** — يحتاج تطبيق جوال مخصص أو خدمة FCM.

لا شيء أعلاه تم "التظاهر" بأنه يعمل — الواجهة تعرض بوضوح أن التنبيه أُنشئ داخليًا فقط.
